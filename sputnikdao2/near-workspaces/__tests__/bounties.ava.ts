import { Workspace, BN, NearAccount, captureError, toYocto, tGas } from 'near-workspaces-ava';
import { workspace, initStaking, initTestToken } from './utils';

workspace.test('Bounty claim', async (test, {root, dao }) => {
    
});